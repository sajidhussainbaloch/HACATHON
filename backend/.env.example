# RealityCheck AI — Backend Configuration
# ────────────────────────────────────────────────

# ── AI Model API Keys ────────────────────────────────────────────────────────
# Cloudflare Account ID (Workers AI)
CLOUDFLARE_ACCOUNT_ID=your_cloudflare_account_id_here
# Cloudflare API Token (Workers AI → Read)
CLOUDFLARE_API_TOKEN=your_cloudflare_api_token_here
# Hugging Face API Token (Inference API — free tier)
HUGGINGFACE_API_TOKEN=your_huggingface_token_here
# Optional alias used by Student Assistant module (falls back to HUGGINGFACE_API_TOKEN)
HF_API_KEY=

# ── Authentication & Security ───────────────────────────────────────────────────
# Generate SECRET_KEY with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=generate_a_random_secret_key_here

# SQLite database path (default: ./realitycheck.db)
DATABASE_URL=sqlite:///./realitycheck.db

# ── Email (SMTP) ─────────────────────────────────────────────────────────────
# For Gmail:
#   1. Enable 2-factor authentication on your Google account
#   2. Go to https://myaccount.google.com/apppasswords
#   3. Create an app password and use it below (NOT your regular password)

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_gmail_app_password_here
FROM_EMAIL=your_email@gmail.com
FROM_NAME=RealityCheck AI
